extern libc {
  fn printf(format *u8, ...) i32;
}

fn fib(n int) int {
  if n <= 1 {
    return n;
  }
  return fib(n - 1) + fib(n - 2);
}

fn main() i32 {
  n := 40;
  result := fib(n);
  libc.printf("%d: %d", n, result);
  return 0;
}
